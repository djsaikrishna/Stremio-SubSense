# SubSense Configuration

# Server port (default: 3100)
PORT=3100

# Public Base URL for the addon (used for subtitle proxy URLs sent to Stremio)
# - For local development: leave commented out (will default to http://127.0.0.1:PORT)
# - For production: set to your public URL (e.g., https://subsense.yourdomain.com)
# SUBSENSE_BASE_URL=https://subsense.yourdomain.com

# Logging level: debug, info, warn, error
LOG_LEVEL=debug

# Maximum subtitles to return per request (default: 30)
# Higher values may make Stremio UI slow
MAX_SUBTITLES=10

# Subtitle sources (comma-separated) (default: all available sources)
# Available: opensubtitles, subdl, subf2m, podnapisi, gestdown, animetosho
SUBTITLE_SOURCES=opensubtitles,subdl,subf2m,podnapisi,gestdown,animetosho