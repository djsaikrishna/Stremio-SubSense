'use strict';

module.exports = {
    stremio: require('./stremio'),
    health: require('./health'),
    configApi: require('./config-api'),
    proxy: require('./proxy'),
    statsApi: require('./stats-api')
};
